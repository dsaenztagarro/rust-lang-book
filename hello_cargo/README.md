# hello_cargo

[The Cargo Book](https://doc.rust-lang.org/cargo/)

```bash
$ cargo build
$ ./target/debug/hello_cargo
$ cargo run
$ cargo check
$ cargo build --release
$ ./target/release/hello_cargo
```

